/**
 * sf telegram — placeholder for open-source build
 * Full Telegram bot integration is available via: npm i -g @spfunctions/cli
 */

export async function telegramCommand(_opts?: Record<string, unknown>): Promise<void> {
  console.log('Telegram bot requires the full CLI install:')
  console.log('  npm i -g @spfunctions/cli')
  console.log('  sf telegram')
}
