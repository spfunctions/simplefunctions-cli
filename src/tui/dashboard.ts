/**
 * TUI Dashboard — placeholder for open-source build
 * Full interactive dashboard is available via: npm i -g @spfunctions/cli
 */

export async function startDashboard(_opts?: Record<string, unknown>): Promise<void> {
  console.log('Interactive TUI dashboard requires the full CLI install:')
  console.log('  npm i -g @spfunctions/cli')
  console.log('  sf dashboard')
}
